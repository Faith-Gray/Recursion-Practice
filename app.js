//for loop

function recursionLoop(x)

  if (x === 0 || 1) {
    return [0]
    }
  elseif (x === 2) {
    return [0, 1]
    }
  else {
  let arr = [0, 1];
  for (let i = 2; i < x; i++) {
  push[arr[n]+ arr[n-1];
  }
  return arr;
} 
